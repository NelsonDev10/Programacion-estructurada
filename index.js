const username = "user"

const name = "nelson"


//primero agregamos una nuevo request con git add .

//luego agregamos un comentario con git commit -m "tu comentario"

//finalmente apuntamos a la rama que queremos subir nuestro nuevos cambios con git push -u origin main